name: Postmortem

on:
  issues:
    types: [closed]

jobs:
  postmortem:
    runs-on: ubuntu-latest
    if: contains(github.event.issue.labels.*.name, 'incident')
    steps:
      - uses: actions/checkout@v3
      - name: Generate postmortem
        uses: actions/github-script@v6
        with:
          script: |
            console.log('Postmortem template would be generated here');
            // Create postmortem document from template
