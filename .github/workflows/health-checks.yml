name: Health Checks

on:
  schedule:
    - cron: '*/5 * * * *'  # Every 5 minutes

jobs:
  health:
    runs-on: ubuntu-latest
    steps:
      - name: Check health endpoint
        run: |
          curl -f http://localhost:4173/health || echo "No health endpoint"
